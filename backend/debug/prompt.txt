import React, { useEffect, useState } from 'react';
import axios from 'axios';
import { Link, useParams, useNavigate } from 'react-router-dom';

const ReadmeList = () => {
  const [files, setFiles] = useState([]);
  const [error, setError] = useState(null);
  const { folderPathParam } = useParams();
  const baseFolderPath = process.env.NODE_ENV === 'development' 
    ? '/home/musong/Documents/record-web/backend/md' 
    : '/root/home/record-web/backend/md';

  const folderPath = folderPathParam ? `${baseFolderPath}/${folderPathParam}` : baseFolderPath;
  const navigate = useNavigate();

  useEffect(() => {
    const fetchData = async () => {
      const devUrl = 'http://localhost:32102';
      const prodUrl = 'http://www.druggableprotein.com:32102';
      const baseUrl = process.env.NODE_ENV === 'development' ? devUrl : prodUrl;
      const endpoint = '/file/list-files-and-folders';
      const url = `${baseUrl}${endpoint}?folder_path=${folderPath}`;
      console.log(url)
      try {
        const response = await axios.get(url);
        if (response.data.items && Array.isArray(response.data.items)) {
          setFiles(response.data.items);
        } else {
          throw new Error("API response is not an array");
        }
      } catch (error) {
        console.error("Error fetching files:", error);
        setError(error);
      }
    };

    fetchData();
  }, []);

  return (
    <div>
      {error ? (
        <div>Error: {error.message}</div>
      ) : (
        <ul>
          {files.map((file, index) => (
            <li key={index}>
              <Link to={`/readme/${encodeURIComponent(file.name)}`}>{file.name}</Link>
            </li>
          ))}
        </ul>
      )}
    </div>
  );
};

export default ReadmeList;



please change and only change the above file
as you can see, the backend api is http://localhost:32102/file/list-files-and-folders?folder_path=/home/musong/Documents/record-web/backend/md
and the json data is 
{
    "items": [
        {
            "name": "/home/musong/Documents/record-web/backend/md/tree.md",
            "type": "file"
        },
        {
            "name": "/home/musong/Documents/record-web/backend/md/readme.md",
            "type": "file"
        },
        {
            "name": "/home/musong/Documents/record-web/backend/md/treeTraversal.svg",
            "type": "file"
        },
        {
            "name": "/home/musong/Documents/record-web/backend/md/graph.svg",
            "type": "file"
        },
        {
            "name": "/home/musong/Documents/record-web/backend/md/graph.md",
            "type": "file"
        },
        {
            "name": "/home/musong/Documents/record-web/backend/md/duanxueqi",
            "type": "folder"
        },
        {
            "name": "/home/musong/Documents/record-web/backend/md/code_month_7.md",
            "type": "file"
        }
    ]
}

for all folders, when i click the folder, then i will goes to readme/folder, for example, duanxueqi is a folder, when i click it, then i am direct to
readme/duanxieqi, and the folders and files are listed under duanxueqi just like readme,
you should note that for a file, when i click for example, tree.md, it goes to http://localhost:3000/readme/%2Fhome%2Fmusong%2FDocuments%2Frecord-web%2Fbackend%2Fmd%2Ftree.md

and the readme file is rendered(i have write the component)

files and folders achieve different behavior, rewrite the file!